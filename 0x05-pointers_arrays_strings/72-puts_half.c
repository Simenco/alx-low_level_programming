#include "main.h"

/**
 * puts2 - prints every other character of string
 * @str: the string input
 * Return: void
 */
void puts_half(char *str)
{
	int i;
	int len = ;
	int n = (len / 2) + 1;

	for (len = 0; len < str[i]; len++)
	{

	}
	for (i = n; i <= len; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
